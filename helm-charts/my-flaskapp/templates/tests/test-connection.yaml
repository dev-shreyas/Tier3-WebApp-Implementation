apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-flaskapp.fullname" . }}-test-connection"
  labels:
    {{- include "my-flaskapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-flaskapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
